const initialState = {

    userId: ''
};


export const landingPageReducer = (state = initialState, action) => {
    switch (action.type) {

        default: {
            return state;
        }
    }
};
