# # from django.conf.urls import url
from django.urls import re_path
#
# from .views import SkillProductView
# from django.conf import settings
#
app_name = 'shop'
urlpatterns = [
    # re_path(r'^skill-product/$',SkillProductView.as_view()),
]
